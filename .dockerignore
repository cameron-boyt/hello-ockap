# .dockerignore — controls what the BUILDER receives.
# It is not .gitignore, and neither removes an already-committed secret.
.git
.venv                 # ← must be your ACTUAL venv directory name
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.github