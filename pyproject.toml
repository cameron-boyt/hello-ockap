[project]
name = "hello-ockap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cameron Boyt", email = "cameronboyt@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hello-ockap = "hello_ockap:main"

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.2",
]
